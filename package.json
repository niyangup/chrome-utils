{
  "name": "chrome-utils",
  "version": "1.0.0",
  "description": "Chrome Utils - 自定义 Chrome 插件",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test --experimental-strip-types tests/apiUsage.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src tests vite.config.ts --deny-warnings --format unix"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.6",
    "oxlint": "^1.78.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  },
  "private": true,
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
